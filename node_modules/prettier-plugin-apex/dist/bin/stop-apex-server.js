#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs"));
const helpers_1 = require("yargs/helpers");
const http_server_1 = require("../src/http-server");
async function teardown(host, port) {
    await (0, http_server_1.stop)(host, port);
}
if (require.main === module) {
    // Support calling this directly
    (0, yargs_1.default)((0, helpers_1.hideBin)(process.argv))
        .command("$0", "stop the built-in parsing server", {
        host: {
            alias: "h",
            default: "localhost",
        },
        port: {
            alias: "p",
            default: 2117,
        },
    }, (argv) => {
        teardown(argv.host, argv.port);
    })
        .help()
        .parse();
}
module.exports = teardown;
